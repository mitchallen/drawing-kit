# Route the @mitchallen scope to GitHub Packages. No secret here: supply the
# token via ~/.npmrc, e.g. //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
@mitchallen:registry=https://npm.pkg.github.com
